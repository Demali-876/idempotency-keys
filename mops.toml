[package]
name = "idempotency-keys"
version = "1.0.0"
description = "UUID V4 (RFC4122)implementation for idempotent HTTP-Requests"
repository = "https://github.com/Demali-876/idempotent-keys"
keywords = [ "idempotency", "http" ]
license = "MIT"

[dependencies]
base = "0.12.0"